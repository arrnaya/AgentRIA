# Day 1 — data layer (RECON + Subgraph Studio client)
httpx>=0.27
python-dotenv>=1.0

# Testing
pytest>=8.0
pytest-asyncio>=0.24
respx>=0.21

# Added as each subsequent agent lands (see README > Build Timeline):
# langgraph            — pipeline/graph.py (Day 2)
# anthropic            — LLM tool-calling across agents (Day 2)
# fastmcp               — mcp_server/server.py (Day 3)
# x402[hedera]         — hedera/x402_client.py, mcp_server x402 middleware (Day 3)
# hiero-sdk-python      — hedera/wallet.py, hedera/hcs_logger.py (Day 3-4)
# websockets            — pipeline/ws_server.py (Day 2)
